import Config "src/lib/Verifier";

module {
    public let config: Config.Config = {
        scorerId = <NUMBER>;
        scorerAPIKey = "<KEY>";
        scorerUrl = "https://api.scorer.gitcoin.co";
    };
}